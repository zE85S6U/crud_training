<!--ヘッダー-->
<?= $this->fetch('header.phtml') ?>
<div class="container">
    <h3>購入確認</h3>

    <!-- カート内商品一覧 -->
    <div class="border border-secondary rounded  m-1 ">
        <div class="col-sm-12 mb-4">
            <?php foreach ($_SESSION['cart'] as $index => $item) : ?>
                <div class="row">
                    <ul class="list-group flex-fill">
                        <li class="list-group-item justify-content-between d-lg-flex align-items-center mb-3">
                            <img class="col-lg-2 img-fluid" src="<?= $item['image_dir'] ?>"
                                 alt="商品画像">
                            <span class="col-lg-3">商品名: <?= $item['product_name'] ?></span>
                            <span class="col-lg-2">価格 : <?= yen($item['price']) ?></span>
                            <span class="col-lg-3">数量 :<?= $item['order_quantity'] ?></span>
                            <span class="col-lg-3">小計 :<?= yen($item['miner_total']) ?></span>
                        </li>
                    </ul>
                </div>
            <?php endforeach; ?>
            <!-- 合計金額 --->
            <span class="h4 row pr-4 justify-content-end">合計 :<?= yen($_SESSION['carts']['total']) ?></span>
        </div>
    </div>
    <?php if (isset($_SESSION['user'])) : ?>
        <div class="row m-1">
            <a role="button" class="text-white btn btn-secondary btn-lg btn-block" href="/cart">カートに戻る</a>
        </div>
        <div class="row m-1">
            <a role="button" class="text-white btn btn-primary btn-lg btn-block" href="/order/submit"
               onclick="return confirm('本当に購入しますか？')">購入確定</a>
        </div>
    <?php else: ?>
        購入するには<a href="/login">ログイン</a>してください。
    <?php endif; ?>
</div>

<!--フッター-->
<?= $this->fetch('footer.phtml') ?>
<!-- ヘッダー読み込み -->
<?= $this->fetch('header.phtml') ?>

<!--ニュース-->
<div class="container">
    <div class="d-block d-sm-none">
        <div class="mx-auto">
            <h5>News</h5>
            <?php for ($i = 0 ; $i < 2; $i++): ?>
                <dl>
                    <dt><?= $news[$i]['now'] ?></dt>
                    <dd><?= $news[$i]['nickname'] . 'な' . $news[$i]['product_name'] ?>が仲間入りしました。</dd>
                </dl>
                <hr>
            <?php endfor; ?>
        </div>
    </div>
    <div class="d-none d-sm-block">
        <div class="px-2 py-2 pt-md-3 pb-md-3 mx-auto">
            <h1 class="display-5">News</h1>
            <?php foreach ($news as $headline): ?>
                <dl>
                    <dt><?= $headline['now'] ?></dt>
                    <dd><?= $headline['nickname'] . 'な' . $headline['product_name'] ?>が仲間入りしました。</dd>
                </dl>
                <hr>
            <?php endforeach; ?>
        </div>
    </div>
<!--商品-->
<div class="card-group">
    <?php foreach ($products as $product): ?>
        <div class="cardtext-white col-lg-3 col-md-4 col-sm-12 p-0">
            <a class="text-white" href="/item/<?= $product['product_id'] ?>">
                <img class="card-img" src="<?= '/image/file/' . $product['image_dir'] ?>" alt="商品画像">

                <div class="card-img-overlay">
                    <h5 class="card-title"><?= $product['product_name'] ?></h5>
                    <p class="card-text">price : <?= yen($product['price']) ?></p>
                    <p class="card-text">stock : <?= stock_confirmation($product['stock']) ?></p>
                </div>
            </a>
        </div>
    <?php endforeach; ?>
</div>
<!--フッター-->
<?= $this->fetch('footer.phtml') ?>